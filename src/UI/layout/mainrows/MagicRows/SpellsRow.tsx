import { Datamap } from "../../../../engine/Datamap"

const SpellsRow = (props: {data: Datamap}) => {
    return (
        <div>
            Here is where I list the spells you can cast
        </div>
    )
}

export default SpellsRow